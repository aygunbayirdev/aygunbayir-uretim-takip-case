# Target REST API endpoint — replace with the actual submission server IP and path
API_ENDPOINT=http://your-api-server-ip:port/api/v1/submit

# API authentication key — obtain from the target system
API_KEY=your-production-key-here

# SQLite database file path (relative to backend directory)
DATABASE_URL=sqlite:///./production.db
